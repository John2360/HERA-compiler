#include "Tiger-stdlib-stack-data.hera"

DLABEL(my_string_1) 
    LP_STRING("here come numbers from 40 to 42: ") 

DLABEL(my_string_2) 
    LP_STRING(" ") 

DLABEL(my_string_3) 
    LP_STRING("\012") 

CBON()

MOVE(FP_alt, SP)
INC(SP, 4)
SET(R1, my_string_1)
STORE(R1, 3, FP_alt) 
CALL(FP_alt, print)
LOAD(R2, 3, FP_alt)
DEC(SP, 4) 

// for loop code
    SET(R1, 40)
INC(SP, 1)
STORE(R1, 0, FP) 

LABEL(my_for_cond_1)
    SET(R1, 42)

LOAD(R2, 0, FP)
CMP(R2, R1)
BG(my_for_post_1)
INC(FP, 1)

MOVE(FP_alt, SP)
INC(SP, 4)
//load i from mem
LOAD(R1, 0, FP)
STORE(R1, 3, FP_alt) 
CALL(FP_alt, printint)
LOAD(R2, 3, FP_alt)
DEC(SP, 4) 

MOVE(FP_alt, SP)
INC(SP, 4)
SET(R1, my_string_2)
STORE(R1, 3, FP_alt) 
CALL(FP_alt, print)
LOAD(R2, 3, FP_alt)
DEC(SP, 4) 

MOVE(R2, R2)

DEC(FP, 1)
LOAD(R2, 0, FP)
INC(R2, 1)
STORE(R2, 0, FP) 

BR(my_for_cond_1)
LABEL(my_for_post_1)
DEC(SP, 1)
MOVE(FP_alt, SP)
INC(SP, 4)
SET(R1, my_string_3)
STORE(R1, 3, FP_alt) 
CALL(FP_alt, print)
LOAD(R2, 3, FP_alt)
DEC(SP, 4) 

MOVE(R2, R2)
MOVE(R2, R2)


HALT()
#include "Tiger-stdlib-stack.hera"
