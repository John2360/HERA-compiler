#include "Tiger-stdlib-stack-data.hera"

DLABEL(my_string_1) 
    LP_STRING("*") 

CBON()

    SET(R1, 1)
INC(SP, 1)
STORE(R1, 0, FP) 

LABEL(my_for_cond_1)
    SET(R1, 6)

LOAD(R2, 0, FP)
CMP(R2, R1)
BZ(my_for_post_1)

MOVE(FP_alt, SP)
INC(SP, 4)
SET(R1, my_string_1)
STORE(R1, 3, FP_alt) 
CALL(FP_alt, print)
LOAD(R2, 3, FP_alt)
DEC(SP, 4) 


LOAD(R2, 0, FP)
INC(R2, 1)
STORE(R2, 0, FP) 

BR(my_for_cond_1)
LABEL(my_for_post_1)
DEC(SP, 1)


HALT()
#include "Tiger-stdlib-stack.hera"
