#include "Tiger-stdlib-stack-data.hera"

CBON()

    SET(R1, 1)

CMP(R1, R0)
BZ(my_if_else_1)

LABEL(my_if_then_1)
MOVE(FP_alt, SP)
INC(SP, 4)
    SET(R1, 42)
STORE(R1, 3, FP_alt) 
CALL(FP_alt, printint)
LOAD(R2, 3, FP_alt)
DEC(SP, 3) 


MOVE(R2, R2)
BR(my_if_post_1)

LABEL(my_if_else_1)

BR(my_if_post_1)
LABEL(my_if_post_1)

HALT()
#include "Tiger-stdlib-stack.hera"
